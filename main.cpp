#include <iostream>

int main(){
  long long int a, b;
  scanf("%lld %lld",&a, &b);
  printf("%lld", a/b);
}

//int a;
//scanf("%d", &a);
//printf("%f", (float)a);
//와 같이 실행하면, 정수로 저장되어 있는 값을 실수형(float)으로 명시적으로 변환하여 출력한다.
//어떤 값이나 결과의 데이터형을 강제로 바꾸는 것을 명시적 (데이터)형변환이라고 한다.

