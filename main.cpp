#include <iostream>
int main(){
    int a, b;
    scanf("%d%d",&a, &b);
    printf("%d", a|b);
}
//비트단위연산자~(bitwise not), &(bitwise and), |(bitwise or), ^(bitwise xor),<<(bitwise left shift), >>(bitwise right shift)
//
